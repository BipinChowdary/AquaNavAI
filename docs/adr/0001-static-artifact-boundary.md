# ADR 0001: Static artifact deployment boundary

**Status:** accepted

Cloudflare Pages serves immutable artifacts generated by a local Python engine.
No Python API is deployed in v1. This keeps the professor demo offline-capable,
removes live NOAA/API failure modes, avoids runtime credentials, and separates
research computation from presentation.
